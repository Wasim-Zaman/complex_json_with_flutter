import "package:flutter/material.dart";

class ObjectsGrid extends StatelessWidget {
  const ObjectsGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
